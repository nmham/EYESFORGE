# EYES FORGE Backlog

調査日: 2026-09-10。対象はこのファイルがある Git リポジトリの追跡ファイル全8件
（ゲームHTML、入口HTML、Service Worker、manifest、アイコン3件、.nojekyll）。
周辺の `../../outputs/`、起動・更新CMD、配布ガイド、構文チェック用コピーも確認した。
ゲームHTMLとSWには調査開始前から未コミット変更があったため、それを維持する。

各項目は30分～2時間の独立作業を想定。根拠は現在のコードを静的に確認したもの。
再現テストを実行した項目は明記する。UX・性能の候補は実測・画面検証後に採否を判断する。
参照行は調査時点。H は `peripheral-vision-trainer.html` を指す。
依存「なし」は他のBacklogタスクを先に完了する必要がないことを意味する。
大きな仕様変更を伴う候補では、この1タスクの成果を設計案に限定する。

## P0: バグ・重大な問題

- [x] **P0-01 別アプリのキャッシュ削除を防ぐ** — バグ / 45分 / 依存: なし
  - 根拠: `sw.js:20` は現在のキャッシュ名以外を全削除する。CacheStorageは同一オリジンで共有されるため、別のGitHub Pagesサイト等のオフライン資産も対象になる。
  - 対応: 削除対象を既知のEYES FORGE旧版キャッシュ名に限定する。
  - 完了条件: 他アプリ・現行版を保持し、旧版のみ削除するテスト、削除完了待ち・失敗伝播テストが通る。
  - 今回選定: 高重要度でゲームルールへの影響がなく、依存が少ない。修正前は5テスト中2件で他アプリの削除を再現。
  - 完了: 2026-09-10。`sw.js`の削除条件を既知の旧版名に限定し、`../../outputs/sw.js`にも同じ修正を反映。`tests/service-worker.test.mjs`の5件すべて成功。テスト実行方法は`tests/README.md`。

- [x] **P0-02 保存ランキングの壊れた形式で起動不能になる問題を修正** — バグ / 90分 / 依存: なし
  - 根拠: H:1672-1725。JSONの構文例外だけを捕捉し、`null`、数値、種目ごとの非配列等を検証しない。初期化の`updateOptions()`からも参照される。
  - 対応: 保存値のオブジェクト・配列・各記録の数値を検証し、不正な部分だけを除外する。
  - 完了条件: `null`、壊れたJSON、非配列、欠損行でもメニュー表示・保存が可能で、正常な他種目の記録が残る。
  - 選定理由: 未完了のP0で、他タスクへの依存がなく、読み込み境界の検証に限定できる。ゲームルール変更のリスクが低い。
  - 完了: 2026-09-10。`normalizeRankings`でルート・種目配列・数値項目を検証。不正行だけを除外し、旧記録の省略可能なメタデータを補完。読み込みだけでは保存内容を書き換えない。
  - 検証: 初期16テスト中10失敗を修正前に再現。追加2件を含むランキング18件と既存SW5件の計23件成功。Edgeの分離プロファイルで破損データ4ケースのメニュー・右クリック命中・30秒終了・保存・Retry成功。他種目の正常記録を保持。

## P1: UX・パフォーマンス・体験

- [ ] **P1-01 実経過時間に基づく終了判定** — バグ / 90分 / 依存: なし
  - 根拠: H:2154でsetIntervalが発火するたびに1秒減算。処理遅延やバックグラウンド制限で実時間とずれる。
  - 対応: 開始時刻から残時間を導出し、期限後のクリックも終了判定する。裏タブでも時間は進む現行意図を維持。
  - 完了条件: 30/60/120秒、遅延コールバック、終了直前入力で終了・保存が一度だけ発生する。

- [ ] **P1-02 設定操作と開始ショートカットの競合を修正** — UX / 60分 / 依存: なし
  - 根拠: H:2262。Enter/Spaceの対象要素を見ずに開始するため、モードボタンやスライダーのキー操作でも開始する。
  - 対応: 操作部品上とキーリピートを除外し、開始時にブラウザ標準動作との二重処理を防ぐ。
  - 完了条件: キーボードで種目・音量を変更でき、Startと画面背景の開始操作は一度だけ動く。

- [ ] **P1-03 次の的の出現予約をセッション終了時に解除** — バグ / 60分 / 依存: なし
  - 根拠: H:2107,2135のsetTimeoutはIDを保持しない。終了後すぐ再開始すると旧セッションの予約が新しい的を消して作り直す可能性がある。
  - 対応: 出現予約を管理し、終了・メニュー・再開始で解除する。
  - 完了条件: ヒット直後のEscape→再開始で旧予約が動かず、通常の補充間隔は変わらない。

- [ ] **P1-04 低い画面と拡大表示でメニューを到達可能にする** — UX / 120分 / 依存: なし
  - 根拠: Hのbody/stageはoverflow:hidden、stageはmin-height:520px。menuにスクロールがなく、設定とランキングが縦に積まれる。
  - 対応: メニュー内の縦スクロールとStart・同意ボタンへの到達性を整える。
  - 完了条件: 1280x720、390x844、844x390、200%ズームで同意・Start・Retryが操作できる画面検証。

- [ ] **P1-05 リサイズ後の的を表示領域内に保つ** — バグ / 90分 / 依存: なし
  - 根拠: H:1981でpx座標を確定し、resize処理がない。縮小・画面回転で的が領域外に残り得る。
  - 対応: リサイズ時に的とクリック判定座標を一緒に再配置する。
  - 完了条件: 全種目で的が画面内に残り、APMの順番・ヒット数は維持される。

- [ ] **P1-06 動きを減らす設定を正しく反映する** — アクセシビリティ / 90分 / 依存: なし
  - 最新方針: ユーザーの環境間でゲームの動きを揃える要望により、ゲーム内の回転・的・波紋はOS設定にかかわらず通常のアニメーションを使用する。reduce対応はメニュー登場時の動きに限定。Edgeで両設定の回転8秒・波紋360msを確認済み。将来の低刺激オプションはゲーム内で明示的に選べる設計を検討する。
  - 根拠: H:1208で全アニメーションを1msにするが、無限反復はそのまま。H:1885のJS中心移動も継続する。
  - 対応: 装飾の無限アニメーションを停止し、的・メニューの可視状態を維持する。明示的な中心移動モードの扱いは設定として整理する。
  - 完了条件: reduced-motionで高速反復がなく、的と同意画面が見え、モードの有効状態と実際の動きが一致する。
  - 部分対応: 2026-09-12。友人環境の高速回転・波紋消失の報告に対し、一律1ms指定を撤去。装飾回転を停止し、的・メニューは静止表示、波紋は360msで小さく拡大・フェードするよう修正。Edgeでreduce/no-preference両方の表示・命中・波紋・後片付けを確認。明示的なJS中心移動の設定方針は未変更のため、この項目は未完了のまま。

- [ ] **P1-07 カーソル更新の重複と描画コストを計測・削減** — パフォーマンス / 120分 / 依存: なし
  - 根拠: H:2242でpointermoveとpointerrawupdateの両方が同じDOM書き込みを行う。cursor-aimに影とfilterがある。
  - 対応: 対応環境では入力イベントを一本化し、録画・性能記録で影の描画コストを比較する。
  - 完了条件: 高頻度入力で重複更新を除去し、動作検証で追従と照準選択を維持。性能は実測値を残す。

- [ ] **P1-08 エラーレスポンスで正常なオフライン資産を上書きしない** — バグ / 90分 / 依存: なし
  - 根拠: `sw.js`のfetchは全GETレスポンスをキャッシュし、404/500も保存する。書き込みPromiseをイベント寿命に接続せず、失敗も捕捉しない。
  - 対応: キャッシュ対象を同一オリジンの成功応答に限定し、書き込み失敗を処理する。
  - 完了条件: 200→500→オフラインでも正常資産が残り、別オリジン・POSTは対象外。容量不足で未処理拒否を出さない。

- [ ] **P1-09 補助パネルのキーボード開閉とフォーカスを整える** — アクセシビリティ / 90分 / 依存: なし
  - 根拠: H:2311のCursorは表示クラスだけを変更し、aria-expanded/controlsやフォーカス復帰がない。Escapeは常にゲームを終了する。
  - 対応: パネル開閉状態を通知し、Escapeで最前面パネルを閉じ、閉じたら呼び出し元に戻す。
  - 完了条件: Cursor/更新情報がTabとEscapeで操作でき、プレイ中の閉じる操作でミスや意図しない中断が発生しない。

- [ ] **P1-10 ランキングの値をHTMLとして解釈しない** — セキュリティ / 60分 / 依存: なし
  - 根拠: H:1725がlocalStorage由来の値をinnerHTMLへ補間する。外部入力経路は現状確認されていないが、改変された保存値をHTMLとして表示できる。
  - 対応: セル要素を生成し、記録値はtextContentで挿入する。
  - 完了条件: HTMLタグやイベント属性を含む保存値で要素生成・スクリプト実行が起きず、通常の表示は同じ。

- [ ] **P1-11 音声初期化失敗でゲーム処理が中断しないようにする** — バグ / 60分 / 依存: なし
  - 根拠: H:1539でAudioContext生成とresume()の失敗を捕捉しない。startGameはplaySoundの後に開始状態を設定する。
  - 対応: 音声作成失敗・再開拒否時に無音でゲームを継続する。
  - 完了条件: API不在、コンストラクタ例外、resume拒否、音量0でも開始・ヒット・終了が動く。

- [ ] **P1-12 的配置の制約が満たせない場合を定義する** — UX / 120分 / 依存: なし
  - 根拠: H:1936の試行上限後はclamp配置で中央除外・GridShotの非重複条件を再検証しない。APM範囲の下限も的サイズで押し広げられる。
  - 対応: 小画面での制約緩和順序を定義し、収まる領域を使う。APMの重なり許可は維持する。
  - 完了条件: 最小画面・最大サイズ・最大除外範囲で到達可能な的が出て、配置が破綻しない境界テスト。

- [ ] **P1-13 保存容量不足時も新しい記録をセッション内で保持する** — バグ / 90分 / 依存: なし
  - 根拠: P0-02検証中に追加発見。`writeRankings`は書き込み失敗時もメモリへ反映するが、次の`readRankings`が読める古い保存値でメモリを上書きする。
  - 再現: getItemが既存1件を返しsetItemのみ容量不足で失敗する条件で、saveRankingは2件を返すが直後のreadRankingsは1件へ戻る。読み書き両方が拒否される場合とは異なる。
  - 対応: 未保存記録の状態を管理し、メモリ上の新しい記録を維持する。永続保存できていないことを控えめに表示する。
  - 完了条件: 書き込みだけ失敗しても結果・メニュー・再プレイで記録が残り、再保存成功時に重複がない。複数タブとの整合性も確認する。

## P2: テスト・リファクタリング・保守性

- [ ] **P2-01 全種目の基本操作のブラウザ回帰テスト** — テスト不足 / 120分 / 依存: なし
  - 根拠: 調査時にリポジトリ内の自動テスト環境なし。H:2028-2189に種目分岐が集中する。
  - 対応: 注意同意、通常命中、GridShot3個維持、APM5→1、Survival3ミス終了を自動化する。
  - 完了条件: 4種目で左・右クリック、終了時一回の記録保存を検証し、実行手順を残す。

- [ ] **P2-02 ランキングとAPM計算の境界テスト** — テスト不足 / 90分 / 依存: なし
  - 根拠: H:1663,1690,1798。種目キー、時間、並べ替え、初ヒットからのAPM計算に回帰テストがない。
  - 対応: 30/60/120秒の記録分離、同点順位、上位5件、0ヒット、APM補正をテストする。
  - 完了条件: 仮想時計で期待値を検証し、現在の1.8倍ルールを維持する。

- [ ] **P2-03 リポジトリ用の構文・Lint確認を整備** — コード品質 / 90分 / 依存: なし
  - 根拠: package.json・Lint設定なし。`../../work/syntax-check.js`はゲーム本体の古い抽出コピーで、新機能の検証に使えない。
  - 対応: 実際のHTMLからスクリプトを抽出して検証し、最小のLint設定を導入する。
  - 完了条件: JS構文エラーと未定義変数を失敗扱いにする。無関係な全体整形は行わない。

- [ ] **P2-04 未使用状態と古いタブCSSを整理** — コード品質 / 90分 / 依存: なし
  - 根拠: Hのtab-list/tab-panel、target.warn、targetTimeouts、createTargetのlevel等に旧実装の痕跡がある。
  - 対応: 参照箇所を確認して未使用分だけ除去する。
  - 完了条件: DOM/CSS参照検索と4種目のスモーク確認が通り、操作・見た目が変わらない。

- [ ] **P2-05 スコア計算だけを小さな純粋関数に切り出す** — 保守性 / 90分 / 依存: P2-02
  - 根拠: H:1798の計算がグローバルstateとperformance.nowに直接依存する。
  - 対応: hits/経過時間/種目を引数にし、UI呼び出しは既存関数に残す。
  - 完了条件: 境界テストが通り、単一HTMLを直接開ける配布形式が保たれる。

- [ ] **P2-06 開発・配布のREADMEをリポジトリに追加** — ドキュメント不足 / 60分 / 依存: なし
  - 根拠: リポジトリにREADMEなし。配布ガイドは外側にあり、GitHub側だけを見ると起動・公開対象が不明。
  - 対応: file起動、ローカルHTTP、Pages公開フォルダ、テスト、キャッシュ更新、記録が端末内保存である点を書く。
  - 完了条件: 新規取得したリポジトリだけで起動と確認ができる。公開手順の記述と実際のデプロイは区別する。

- [ ] **P2-07 配布物の同期・ZIP生成を再現可能にする** — 保守性 / 120分 / 依存: なし
  - 根拠: リポジトリ、`../../outputs`、配布ZIPが別に存在し、手動コピー運用。調査時のHTMLとSWは同一ハッシュ。
  - 対応: 正本をリポジトリに定め、明示した出力先に配布ファイルだけを書き出すスクリプトを追加する。
  - 完了条件: 出力ハッシュ一致、テスト等の混入なし、予期しないディレクトリを書き換えない。

- [ ] **P2-08 公開資産とPWAのスモークチェック** — テスト不足 / 120分 / 依存: なし
  - 根拠: indexの転送、manifestの相対URL、SWのAPP_FILESに整合性検証がない。
  - 対応: `/EYESFORGE/`配下で入口・全資産・SW登録・再訪オフラインを確認するテストを追加する。
  - 完了条件: HTTPで初回表示後にオフライン再訪でき、大小文字や欠損資産を失敗として検出する。

- [ ] **P2-09 同一アプリの別設置先のキャッシュ分離を設計** — 保守性 / 60分 / 依存: なし
  - 根拠: `sw.js:1`のキャッシュ名は固定。P0-01修正後も、同一オリジンでEYES FORGEを別パスに設置すると旧版名を共有する。
  - 対応: registration.scopeを含む名前と既存キャッシュ移行方針を設計する。このタスクは設計・再現例まで。
  - 完了条件: 本番/試験版を共存させる名前、更新・ロールバック・旧キャッシュ移行の手順がレビュー可能。
  - 追加発見: P0-01のテスト設計中に確認。旧版名には所有パスがなく、無条件の自動移行は避ける。

## P3: 小機能・将来の改善

- [ ] **P3-01 音量とカーソル設定の保存** — 小機能 / 60分 / 依存: なし
  - 根拠: H:1448以降のstateと初期化で毎回既定値に戻る。
  - 対応: 音量・ミュート・カーソルのみ保存し、不正値では既定値へ戻す。
  - 完了条件: 再訪で復元され、ストレージ無効環境でも動作する。高負荷設定の自動復元はこの項目に含めない。

- [ ] **P3-02 ランキングの書き出し** — 小機能 / 90分 / 依存: なし
  - 根拠: ランキングはlocalStorageのみで、端末変更やデータ消去時の退避手段がない。
  - 対応: 全種目の記録をバージョン付きJSONでダウンロードする。
  - 完了条件: 日本語・空データ・保存不可環境を扱え、現在の記録を変更しない。取り込みは別タスク。

- [ ] **P3-03 休憩の控えめな通知を追加** — 小機能 / 90分 / 依存: なし
  - 根拠: 注意画面はあるが、連続Retry回数や連続プレイ時間を記録しない。
  - 対応: 結果画面で一定の累計プレイ時間後に休憩案内を表示し、通知設定を持つ。
  - 完了条件: プレイ中に割り込まず、タイマーとスコアに影響しない。治療・能力向上の効果は断定しない。

- [ ] **P3-04 結果にミス数と実プレイ時間を追加** — 小機能 / 60分 / 依存: なし
  - 根拠: H:2165と結果HTMLにはAccuracyだけがあり、Survivalの早期終了時も設定時間のみがランキングに表示される。
  - 対応: 結果に実際のミス数と経過秒数を表示する。種目キーと既存順位ルールは維持する。
  - 完了条件: 0ヒット・時間終了・3ミス早期終了の数値が正しい。

- [ ] **P3-05 種目ごとの設定条件を記録・比較する設計** — 将来改善 / 90分 / 依存: なし
  - 根拠: H:1663のキーは種目・中心移動有無・秒数だけ。的サイズや色、散らばりの異なる記録が同順位表に混ざる。
  - 対応: 保存すべき設定と表示フィルター、既存記録の扱いを設計する。順位体系の変更実装は別タスクにする。
  - 完了条件: 旧データを失わない移行案と、設定別比較のUI案を文書化する。

## 検証と制約

- P0-01、P0-02完了。全29件中2件完了、27件未着手。次はP1-01（実経過時間による終了判定）を推奨する。
- 自動検証は実際のSWをNode VMで実行するライフサイクルテスト。ブラウザの実CacheStorageやオフライン遷移はP2-08で扱う。
- `node --test tests/*.test.mjs`: 23/23成功（ランキング18、SW5）。P0-01修正前はSW3/5で問題を再現。
- `node tests/rankings.browser.cjs`: Edgeで破損ストレージ4ケース成功。ブラウザテストの初回実行ではCSS大文字変換と期待文字列の不一致を検出し、DOMのtextContentを確認するようテストを修正した。
- `node --check sw.js`、両HTMLから抽出したインラインJSの`vm.Script`構文検証、manifestのJSON解析、`git diff --check`: 成功。
- 本体は静的HTMLでビルド設定・Lint設定なし。そのためビルド・Lintは未実施。
- リポジトリ版とoutputs版のSWおよびゲームHTMLの内容一致を確認。P0-02は両方のゲームHTMLに反映。配布ZIPは再生成していないため、次回配布時にP2-07の同期確認が必要。
- パフォーマンスの数値測定や全画面サイズの視覚検証は今回未実施。
- 公開・GitHubへのpushはこのローカル改善には含めない。
